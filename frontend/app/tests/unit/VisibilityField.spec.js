import { mount } from '@vue/test-utils';
import VisibilityField from "@/components/VisibilityField.vue";

describe("VisibilityField.vue", () => {
	it("does something", () => {
		const wrapper = mount(VisibilityField);
	});
});